spring:
  application:
    name: ${STUDENT_APPLICATION_NAME}
  config:
    import:
      - optional:file:.env[.properties]
      - optional:nacos:${NACOS_SERVER_ADDR}
  datasource:
    url: ${STUDENT_DATASOURCE_URL}
    username: ${STUDENT_DATASOURCE_USERNAME}
    password: ${STUDENT_DATASOURCE_PASSWORD}
    driver-class-name: ${STUDENT_DATASOURCE_DRIVER_CLASS_NAME}

server:
  port: ${STUDENT_SERVER_PORT}

dubbo:
  application:
    name: ${STUDENT_DUBBO_APPLICATION_NAME}
  protocol:
    name: dubbo
    port: ${STUDENT_DUBBO_PROTOCOL_PORT}
  registry:
    address: nacos://${NACOS_SERVER_ADDR}
  scan:
    base-packages: com.xqy.sms.student.provider.service # Dubbo服务接口实现类所在的包

