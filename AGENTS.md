# AI 任务执行约定

本文件是仓库级约定，适用于后端 Maven 多模块项目和 `sms-ui` 前端。目标是在保证正确性的前提下减少无关扫描、修改和构建，缩短 AI 任务执行时间。

## 总体原则

- 先明确任务目标、入口文件、影响边界和验收条件，再开始搜索或修改。
- 默认只读取任务相关文件，以及从任务入口出发的直接调用链：调用方、被调用方、相关 DTO/类型、配置和测试。不要为了熟悉项目而全量扫描。
- 优先使用 `rg`、IDE 索引或构建工具的模块信息定位代码；避免无目的地遍历 `target`、`node_modules`、生成文件、日志和二进制文件。
- 默认不执行全仓库构建、全量测试或全量静态检查。只有任务涉及公共接口、跨模块依赖、构建配置、数据库结构，或局部验证不足以证明正确性时，才扩大范围。
- 不修改与任务无关的代码、格式、依赖版本、生成文件或配置。发现已有用户改动时保留并在其基础上工作。
- 修改前先理解现有实现和约定；优先采用已有工具、组件、命名和错误处理方式，不新增重复抽象。
- 每次任务结束时说明实际扫描范围、修改文件和执行过的验证；未执行的更大范围验证要明确指出原因。

## 任务范围与调用链

1. 从用户描述、报错、路由、类名、组件名或文件名确定最小入口集合。
2. 沿直接调用链向外扩展一层：后端通常包含 Controller/Service/Repository、DTO/实体和对应测试；前端通常包含页面、组件、状态/请求层、类型和样式。
3. 只有出现以下信号时才继续扩展：公共类型或方法被多个模块使用、接口响应/请求发生变化、数据库字段或 SQL 变化、认证/权限/全局状态变化、构建或运行时配置变化、现有局部验证失败。
4. 对纯样式、文案、局部逻辑或单文件缺陷，保持单文件或最小文件集修改，不追踪无关业务链路。

## 后端（Java/Maven）

- 先定位具体模块和入口，再阅读该模块的 `pom.xml`、相关类和测试；不要默认读取所有 Maven 模块。
- 修改优先限制在受影响模块及其直接依赖模块。若只改实现细节，优先运行该模块的编译或相关测试。
- 后端最小验证顺序：
  1. 语法和类型检查：`mvn -pl <module> -am -DskipTests compile`（仅在需要解析直接依赖时使用 `-am`）。
  2. 相关测试：`mvn -pl <module> -Dtest=<TestClass>[#<method>] test`。
  3. 仅当接口、公共模型、数据库迁移或跨模块行为改变时，才补充受影响模块的集成测试或更大范围验证。
- 不因小改动自动执行根目录全量 `mvn test`、打包、安装或发布；如确有必要，先说明范围和原因。
- 涉及 SQL、表结构或 ORM 映射时，只检查对应初始化脚本、迁移脚本、实体/Mapper 和直接调用方，并验证字段名、类型、空值和索引的一致性。

## 前端（`sms-ui`）

- 先检查 `sms-ui/package.json` 的脚本和相关页面/组件，再沿请求函数、状态管理、类型和样式的直接调用链定位。
- 修改优先限制在受影响页面、组件、hooks/服务、类型和样式文件，不扫描或重构整个前端。
- 前端最小验证顺序：
  1. 对改动文件执行项目已有的 lint、typecheck 或格式检查脚本。
  2. 对受影响功能执行对应的单元测试或组件测试；没有测试时进行最小可复现的开发构建或页面验证。
  3. 只有路由、全局状态、公共组件、请求契约或构建配置变化时，才运行更大范围的检查或构建。
- 不因局部 UI 或业务逻辑改动自动执行全量生产构建；如构建耗时或依赖安装会显著增加任务成本，先完成静态和局部验证。
- 保持现有 UI 框架、组件库、状态管理、请求封装和样式约定；不要在局部任务中引入新库或替换工具链。

## 修改与验证纪律

- 先做只读定位，再编辑；编辑后只复查变更文件和直接受影响文件。
- 验证命令应与风险匹配：局部改动做局部验证，契约或基础设施改动才做跨模块验证。
- 优先验证失败路径、边界条件、空值/权限/错误处理以及请求响应兼容性，而不是重复验证未改动的代码。
- 如果无法运行某项验证，记录缺少的环境、依赖或外部服务，并用可行的静态检查或更小范围测试替代；不要假装验证通过。
- 最终报告简要列出：改动摘要、验证命令及结果、未执行的验证和剩余风险。

